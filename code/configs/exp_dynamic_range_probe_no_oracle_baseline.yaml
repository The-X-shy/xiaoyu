experiment:
  name: "exp_dynamic_range_probe_no_oracle_baseline"
  seed: 20260210
  base_config: "configs/base_no_oracle.yaml"

sweep:
  type: "pv_amplitude"
  pv_min: 0.5
  pv_max: 12.0
  pv_step: 1.0
  n_repeats: 3

output:
  raw_dir: "outputs/raw/dynamic_range_probe_no_oracle_baseline"
  table: "outputs/tables/dynamic_range_probe_no_oracle_baseline_results.csv"

