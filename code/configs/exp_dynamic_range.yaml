experiment:
  name: "exp_dynamic_range"
  seed: 20260210
  base_config: "configs/base.yaml"

sweep:
  type: "pv_amplitude"
  pv_min: 0.5
  pv_max: 30.0
  pv_step: 0.5
  n_repeats: 20

output:
  raw_dir: "outputs/raw/dynamic_range"
  table: "outputs/tables/dynamic_range_results.csv"
